// Dead-code sweep removed every helper here (no callers). Kept as an empty
// module because lib/payments/index.ts `export *`s it; file removal is a
// follow-up alongside that barrel line.
export {};

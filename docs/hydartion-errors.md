"Autofill Smartly - JobFill" is causing problems for me by adding extra form attributes and input fields during hydration. This causes hydration mismatches between the server and client rendered HTML.

"Colorzilla" is also adding extra attributes to the code during hydration, specifically color-related attributes and styles that weren't present in the server HTML. This creates hydration errors as the client and server HTML don't match.

Just uninstall all chrome extensions
